using System;

namespace HANvOO
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello Jann, This is 'HANvOO'!");
            int index =0;
            foreach( var arg in args)
            {
		// string name = arg.Name;
                Console.WriteLine("Argument {0}: {1} ", index, arg);
                index++;
            }
        }
    }
}
